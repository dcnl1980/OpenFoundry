version: "3.9"

x-common: &common
  restart: unless-stopped
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

services:
  # ── PostgreSQL ──────────────────────────────────────────────
  postgres:
    <<: *common
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: openfoundry
      POSTGRES_PASSWORD: openfoundry
      POSTGRES_DB: openfoundry
      POSTGRES_MULTIPLE_DATABASES: "openfoundry_auth,openfoundry_connector,openfoundry_dataset,openfoundry_streaming,openfoundry_query,openfoundry_pipeline,openfoundry_ontology,openfoundry_fusion,openfoundry_ml,openfoundry_ai,openfoundry_workflow,openfoundry_notebook,openfoundry_apps,openfoundry_report,openfoundry_code,openfoundry_market,openfoundry_nexus,openfoundry_geo,openfoundry_notify,openfoundry_audit"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openfoundry"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Redis ───────────────────────────────────────────────────
  redis:
    <<: *common
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── NATS (JetStream) ───────────────────────────────────────
  nats:
    <<: *common
    image: nats:2-alpine
    ports:
      - "4222:4222"
      - "8222:8222"
    command: --jetstream --store_dir /data --http_port 8222
    volumes:
      - nats_data:/data
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── MinIO (S3-compatible) ──────────────────────────────────
  minio:
    <<: *common
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin &&
      mc mb --ignore-existing local/openfoundry &&
      mc mb --ignore-existing local/openfoundry-datasets &&
      mc mb --ignore-existing local/openfoundry-notebooks &&
      mc mb --ignore-existing local/openfoundry-artifacts
      "

  # ── Meilisearch ────────────────────────────────────────────
  meilisearch:
    <<: *common
    image: getmeili/meilisearch:v1.11
    ports:
      - "7700:7700"
    environment:
      MEILI_MASTER_KEY: masterKey
      MEILI_ENV: development
    volumes:
      - meilisearch_data:/meili_data
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:7700/health"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Qdrant (Vector DB) ────────────────────────────────────
  qdrant:
    <<: *common
    image: qdrant/qdrant:v1.12.5
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:6333/healthz"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
  nats_data:
  minio_data:
  meilisearch_data:
  qdrant_data: